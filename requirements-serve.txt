# Inference service. In the container torch is installed separately from the CPU
# index (see Dockerfile) to avoid pulling the CUDA build into the image.
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
transformers==4.41.2
tokenizers==0.19.1
huggingface-hub==0.23.4
sentencepiece==0.2.0
protobuf==4.25.3
httpx==0.27.0          # fastapi TestClient
